# Presentation about Reproducible development environments with the Nix package manager

To build the presentation, [download and install](https://nixos.org/download.html) the Nix package manager, and then run this at the root of the cloned git project:

> $ nix-build

and the pdf file will be at `./result/pres.pdf`
